[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --tb=short
markers =
    network: requires internet access (real GitHub clone)
    slow: loads the local embedding model or indexes a repository
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore::FutureWarning
