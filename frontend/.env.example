# Frontend configuration. Copy to `.env` (or `.env.local`) to override.

# Base URL of the FastAPI backend.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Axios timeout in milliseconds. Indexing large repositories can be slow.
VITE_API_TIMEOUT_MS=180000
